/**
 * Shared between the pre-hydration inline script in `src/app/layout.tsx` and
 * `useDarkMode` so both read/write the same localStorage key.
 */
export const DARK_MODE_STORAGE_KEY = "mux_dark_mode";
